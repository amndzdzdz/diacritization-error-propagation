__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
*.egg-info/
dist/
build/
.env
data/raw/
data/processed/
*.ckpt
*.pt

# NOTE: `insights` and `docs` were previously ignored wholesale, which left
# the per-week plans and the entire experiment log untracked and unbacked.
# Both are core deliverables per CLAUDE.md -- keep them in version control.

# Baseline reproduction: cloned S3PRL toolkit, downloaded/materialized
# datasets, and per-job logs -- all generated by run/prepare_baseline.sh /
# run/train_baseline.slurm, not checked in.
run/s3prl_checkout/
run/s3prl_venv/
run/hf_cache
run/tmp/
run/data/
run/logs/output_logs/*.log
run/logs/error_logs/*.log
!run/logs/output_logs/.gitkeep
!run/logs/error_logs/.gitkeep
